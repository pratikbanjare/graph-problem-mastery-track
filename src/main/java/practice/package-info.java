/**
 * Root package for the graph algorithms library.
 *
 * <p>This package does not define stable entry points directly. Supported library APIs live in
 * subpackages such as {@code practice.graph}, {@code practice.path}, {@code practice.mst},
 * {@code practice.scc}, {@code practice.sort}, and {@code practice.model}. Demo launchers are kept
 * outside {@code src/main/java} under the repository's {@code examples/} tree so they are not
 * published as part of the library artifact.
 */
package practice;
